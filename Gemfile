source "https://rubygems.org"

# github-pages pins Jekyll and its plugins to the versions used by GitHub Pages,
# so local builds behave exactly like the deployment build.
gem "github-pages", group: :jekyll_plugins

# Required by Jekyll's built-in development server on Ruby >= 3.0.
gem "webrick"
